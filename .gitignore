# Paid product PDFs — never commit (would expose products if the repo is public)
*.pdf

# Python build artifacts
__pycache__/
*.pyc
*.pyo

# Fonts / render scratch (regenerated by build scripts)
_fonts/
_render/

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Video pipeline scratch (regenerable / large binaries)
video/assets/
video/out/
*.mp4
*.mov
*.mp3
*.wav
